# Grass textures

Intentionally empty. The current production grass GLB uses procedural,
terrain-tinted color in `../shaders/grass_glb_frag.glsl`; it has no reusable
external grass image texture. Do not copy GpuHD terrain-material grass maps into
this directory because 117HD remains authoritative for terrain materials.

